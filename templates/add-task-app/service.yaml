apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-svc
spec:
  type: ClusterIP
  selector:
    app: {{.Release.Name }}-lab
  ports:
  - port: {{ .Values.add.image.servicePort }}
    targetPort: {{ .Values.add.image.containerPort }}  
