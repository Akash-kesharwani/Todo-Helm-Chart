apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-todo
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}-lab
  ports:
  - port: {{ .Values.delete.image.servicePort }}
    targetPort: {{ .Values.delete.image.containerPort }}
